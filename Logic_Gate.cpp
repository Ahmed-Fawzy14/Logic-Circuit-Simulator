//
// Created by Fawzy on 3/7/2024.
//

#include "Logic_Gate.h"
#include "circuit.h"

#include <algorithm>
using namespace std;
//    vector<tuple<char, bool, int>> cirInputs;
/*void Logic_Gate::sort_Cir_inputs(circuit c) {
sort((c.getcirInputs()).begin(), (c.getcirInputs()).end(),
     [](const tuple<string, bool, int> &a, const tuple<string, bool, int> &b)
     {
        return (get<2>(a) < get<2>(b));
     });
}*/
